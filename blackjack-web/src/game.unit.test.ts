/// <reference types="jest" />
import { BlackjackGame } from './game';

// Ce fichier est volontairement gardé pour compatibilité, mais la suite
// d’unit-tests réellement exécutée est celle de game.test.fixed.ts.
describe('BlackjackGame (unit) - legacy suite disabled', () => {
  test('disabled placeholder', () => {
    expect(true).toBe(true);
  });
});

